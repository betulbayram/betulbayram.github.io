# Bölüm 15: AI Agent'lar 🤖 - LLM'leri Pasif Bilgeden Aktif Problem Çözücüye Dönüştürmek
Önceki bölümlerde, Büyük Dil Modellerini (LLM'ler) bir API aracılığıyla çağırmayı, onlara "hafıza" (embeddings) ve "bilgi" (RAG) vermeyi öğrendik. Ancak tüm bu etkileşimlerde temel bir dinamik vardı: **Model pasifti.** Biz bir soru sorduk, o da bir cevap üretti. Etkileşim orada sona erdi.

**AI Agent (Yapay Zeka Ajanı)** ise bu dinamiği tamamen tersine çevirir. Bir ajan, otonom bir sistemdir:

* **Algılar (Perceives):** Çevresini, bir sorguyu veya bir durumu analiz eder.
* **Akıl Yürütür (Reasons):** Belirli bir hedefe ulaşmak için bir plan yapar.
* **Eylem Alır (Acts):** Bu planı gerçekleştirmek için araçları (API'ler, veritabanları, kod yorumlayıcıları) kullanır.

Kısacası, bir LLM'e "soru sormayı" bırakıp, ona bir **"görev vermeye"** başladığımız yer burasıdır.

## Ajanların "Zihni": ReAct (Reason + Act) Döngüsü
Bir ajanın otonom çalışmasını sağlayan temel teknik, **ReAct (Akıl Yürütme + Eylem)** olarak bilinen bir prompt (talimat) stratejisidir. Geleneksel bir sorgu, modelden sadece bir cevap ister. ReAct ise modelden bir süreç oluşturmasını ister.

Bu, bir **Düşünce -> Eylem -> Gözlem** döngüsüdür:

1.  **Thought (Düşünce):** Model, nihai hedefe ulaşmak için o anki durumuna bakar ve bir sonraki mantıksal adımın ne olduğuna karar verir. ("Neye ihtiyacım var? Hangi bilgim eksik?")
2.  **Action (Eylem):** Model, bu eksik bilgiyi gidermek veya bir görevi yerine getirmek için elindeki bir **"aracı" (Tool)** kullanmaya karar verir.
3.  **Observation (Gözlem):** Aracın çıktısı (API yanıtı, veritabanı sorgu sonucu, kodun hatası) modele geri beslenir.

Model, bu yeni "gözlem" ile döngünün başına döner (yeni bir "Düşünce" üretir) ve hedefe ulaşana kadar bu süreci tekrarlar.

### Teknik Bir Örnek:
**Hedef (Kullanıcı Sorgusu):** "Şu an Paris'te hava kaç derece ve en son Fransa Grand Prix'sini kim kazandı?"

**Ajanın Çalışma Döngüsü (ReAct):**

1.  **Düşünce:** "Kullanıcı benden iki ayrı bilgi istiyor: Paris'teki hava durumu ve son Fransa F1 yarışının galibi. Önce hava durumunu bulacağım."
2.  **Eylem:** Model, `get_weather` aracını çağırır. JSON çıktısı üretir:
    ```json
    { "tool_name": "get_weather", "arguments": { "city": "Paris" } }
    ```
3.  **Gözlem:** (Sistem, bu JSON'u alır, gerçek API'yi çalıştırır ve sonucu döndürür) "Paris için hava durumu: 18°C, Güneşli."
4.  **Düşünce:** "Hava durumu bilgisini aldım. Şimdi Fransa F1 yarışının galibini bulmam gerekiyor. Bunun için `search_web` aracını kullanmalıyım."
5.  **Eylem:** Model, `search_web` aracını çağırır:
    ```json
    { "tool_name": "search_web", "arguments": { "query": "son Fransa Grand Prix galibi" } }
    ```
6.  **Gözlem:** "Arama Sonucu: Son Fransa Grand Prix'si 2022'de yapıldı ve Max Verstappen kazandı."
7.  **Düşünce:** "Elimde her iki bilgi de var (18°C Paris ve Max Verstappen). Şimdi kullanıcıya nihai cevabı sentezleyip verebilirim."
8.  **Nihai Cevap (Üretim):** "Şu an Paris'te hava 18°C ve güneşli. En son Fransa Grand Prix'sini (2022) ise Max Verstappen kazandı."

## Ajanların "Elleri": Araçlar (Tools) ve Fonksiyonlar (Functions)
Bir ajanın "Eylem" (Action) alabilmesi için "ellere" ihtiyacı vardır. Bu eller, ona dış dünyayla etkileşim kurma yeteneği veren araçlardır.

### OpenAI Functions / Tools (Araçlar)
Bu, bir ajanın araçları kullanması için en yaygın ve güçlü mekanizmadır. OpenAI, modellerini (GPT-3.5/4/4o) özel bir JSON formatında çıktı üretecek şekilde fine-tune etmiştir.

Siz API'ye sadece kullanıcı sorgusunu değil, aynı zamanda ajanın kullanabileceği araçların bir listesini (bir JSON şeması olarak) gönderirsiniz.

Model, ReAct döngüsündeki "Eylem" adımına geldiğinde, metin olarak cevap vermek yerine, tam olarak şu yapıda bir JSON nesnesi döndürür:
```json
{
    "tool_calls": [
        {
            "function": {
                "name": "get_weather",
                "arguments": "{\"city\": \"Paris\"}"
            }
        }
    ]
}
```
Sizin uygulamanız (kodunuz) bu özel JSON'u "yakalar", `name` alanındaki `get_weather` fonksiyonunu gerçekten çalıştırır, `arguments`'ı bu fonksiyona parametre olarak geçer, API'den gelen gerçek sonucu alır ve bu sonucu "Gözlem" olarak modele geri besler.

### Manuel Uygulama (Custom Loop)
Bu, en yüksek düzeyde kontrol sağlayan yöntemdir. Burada, `LangChain` veya `LlamaIndex` gibi bir framework kullanmak yerine, ajanın **Düşünce -> Eylem -> Gözlem** döngüsünü bir `while` döngüsü içinde kendiniz yönetirsiniz.

Sürekli olarak modele bir "Düşünce" için prompt gönderir, modelin cevabını (bir araç çağrısı olup olmadığını) analiz eder, aracı çalıştırır, sonucu alır ve bir sonraki prompt'u oluşturarak döngüyü sürdürürsünüz. `Vercel AI SDK` gibi araçlar, bu döngü sırasında ortaya çıkan "Düşünce" adımlarını kullanıcı arayüzüne (UI) akıcı bir şekilde stream etmek için mükemmeldir.

### Ajanları Barındırma: Yönetilen Platformlar
Bu karmaşık döngüleri ve araç etkileşimlerini yönetmek zorludur. Bu nedenle, tüm bu altyapıyı "hizmet olarak" sunan platformlar kritik öneme sahiptir.

* **Google Vertex AI:** Bu, sadece bir model API'si değil, Google'ın tüm yapay zeka araçlarını (Gemini modelleri dahil) birleştiren tam kapsamlı bir MLOps platformudur. Vertex AI, bu ajanları oluşturmak, dağıtmak ve en önemlisi ölçeklendirmek için gereken tüm yönetilen altyapıyı sunar. Google'ın AI SDK'ları (kullanıcının bahsettiği ADK, muhtemelen AI SDK'nın bir parçası), bu platform üzerinde Gemini'nin akıl yürütme yeteneklerini kullanarak araçlara (Google Arama, Google Workspace vb.) bağlanan ajanlar oluşturmayı kolaylaştırır.
* **OpenAI Assistants API:** OpenAI'nin, bu ajan döngüsünü (ReAct, araç kullanımı, hafıza/konuşma geçmişi) sunucu tarafında sizin için yöneten hazır bir çözümüdür.

### Ajanların Kullanım Alanları ve Geleceği
Ajanlar, yapay zekanın sadece "bilgi" değil, "otomasyon" sağlamasının kapısını aralar.

* **İş Akışı Otomasyonu:** "Yeni bir müşteri e-postası geldiğinde, e-postayı analiz et, CRM'de yeni bir kayıt oluştur, müşteriye bir hoş geldin e-postası taslağı hazırla ve bunu onay için bana Slack'ten gönder."
* **Müşteri Desteği:** Sadece SSS (FAQ) cevaplamakla kalmaz, aynı zamanda kullanıcının hesabını kontrol eder, veritabanından sipariş durumunu çeker ve gerekirse iade işlemini başlatır.
* **Siber Güvenlik:** Bir anomali tespit edildiğinde, ajanın logları analiz etmesi, ilgili IP'yi veritabanında sorgulaması ve tehdit durumunda o IP'yi güvenlik duvarında (firewall) engelleme eylemini gerçekleştirmesi.

Ajanlar, LLM'leri birer "yardımcı pilot" (copilot) olmaktan çıkarıp, belirli görevleri otonom olarak yürütebilen "otopilotlara" dönüştürme yolculuğundaki en önemli adımdır.